class _Config {

	constructor(stringToFormat, bold, italic, strike, underline, lineBreak) {
  	this._stringToFormat = stringToFormat;
  	this._boldSymbol = bold;
    this._italicSymbol = italic;
    this._strikeSymbol = strike;
    this._underlineSymbol = underline;
    this._lineBreakSymbol = lineBreak;
  }
  
}
